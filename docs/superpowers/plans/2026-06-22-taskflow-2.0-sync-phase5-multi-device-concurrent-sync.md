# TaskFlow 2.0 Sync Phase 5: 多设备并发同步实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` or dispatch a fresh `general_purpose_task` subagent per Task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让桌面端支持同时与 2-3 台已配对设备保持同步会话，解决并发写入冲突，并在 UI 上展示每台设备的同步状态。

**Architecture:** 在 `SyncEngine` 之上引入 `SyncPeerManager`，负责维护多个 `SyncSession`/`SyncEngine` 实例的生命周期；本地写操作通过 `SyncScheduler` 广播给所有在线 peer；冲突解决保留 last-write-wins 作为默认策略，但新增版本向量（version vector）检测并发写入并上报 UI。

**Tech Stack:** Electron、TypeScript、better-sqlite3-multiple-ciphers、Zustand、WebSocket/TCP、vitest。

---

## 前置检查

- [ ] **Step 1: 确认 Phase 1-4 基础健康**

Run:
```bash
cd /workspace/taskflow/desktop
npm run typecheck
npm run lint
npm run test
```
Expected: 全部通过。

---

## Task 1: 抽象 `SyncPeer` 与 `SyncPeerManager`

**Files:**
- Create: `desktop/src/main/services/sync/syncPeer.ts`
- Create: `desktop/src/main/services/sync/syncPeerManager.ts`
- Create: `desktop/src/tests/unit/sync/syncPeerManager.test.ts`

- [ ] **Step 1: 定义 `SyncPeer` 类型**

在 `syncPeer.ts` 中：

```typescript
import { EventEmitter } from 'events';
import { SyncSession } from './syncSession';
import { SyncEngine } from './syncEngine';

export type PeerState = 'connecting' | 'handshaking' | 'syncing' | 'idle' | 'error' | 'closed';

export interface SyncPeer {
  deviceId: string;
  session: SyncSession;
  engine: SyncEngine;
  state: PeerState;
  lastSyncAt: number | null;
  error: string | null;
}
```

- [ ] **Step 2: 实现 `SyncPeerManager`**

在 `syncPeerManager.ts` 中：

```typescript
import { EventEmitter } from 'events';
import { SyncPeer, PeerState } from './syncPeer';

export interface PeerManagerState {
  peers: Array<{
    deviceId: string;
    state: PeerState;
    lastSyncAt: number | null;
    error: string | null;
  }>;
}

export class SyncPeerManager extends EventEmitter {
  private peers = new Map<string, SyncPeer>();

  addPeer(peer: SyncPeer): void {
    if (this.peers.has(peer.deviceId)) {
      this.removePeer(peer.deviceId);
    }
    this.peers.set(peer.deviceId, peer);
    this.bindPeerEvents(peer);
    this.emit('stateChanged', this.getState());
  }

  removePeer(deviceId: string): void {
    const peer = this.peers.get(deviceId);
    if (!peer) return;
    peer.session.removeAllListeners();
    peer.engine.removeAllListeners();
    peer.state = 'closed';
    this.peers.delete(deviceId);
    this.emit('stateChanged', this.getState());
  }

  getPeer(deviceId: string): SyncPeer | undefined {
    return this.peers.get(deviceId);
  }

  getPeers(): SyncPeer[] {
    return Array.from(this.peers.values());
  }

  getState(): PeerManagerState {
    return {
      peers: this.getPeers().map((p) => ({
        deviceId: p.deviceId,
        state: p.state,
        lastSyncAt: p.lastSyncAt,
        error: p.error,
      })),
    };
  }

  private bindPeerEvents(peer: SyncPeer): void {
    peer.engine.on('complete', () => {
      peer.state = 'idle';
      peer.lastSyncAt = Date.now();
      this.emit('peerSyncComplete', peer.deviceId);
      this.emit('stateChanged', this.getState());
    });
    peer.engine.on('error', (err: Error) => {
      peer.state = 'error';
      peer.error = err.message;
      this.emit('stateChanged', this.getState());
    });
    peer.session.on('close', () => {
      this.removePeer(peer.deviceId);
    });
  }
}
```

- [ ] **Step 3: 编写 `syncPeerManager.test.ts`**

验证 add/remove/getState 与事件传播。

- [ ] **Step 4: 运行测试**

```bash
cd /workspace/taskflow/desktop
npx vitest run src/tests/unit/sync/syncPeerManager.test.ts
```
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/main/services/sync/syncPeer.ts desktop/src/main/services/sync/syncPeerManager.ts desktop/src/tests/unit/sync/syncPeerManager.test.ts
git commit -m "feat(sync): add SyncPeer and SyncPeerManager for multi-device sessions"
```

---

## Task 2: 本地变更广播给所有在线 Peer

**Files:**
- Modify: `desktop/src/main/services/sync/syncPeerManager.ts`
- Modify: `desktop/src/main/services/sync/syncEngine.ts`
- Create: `desktop/src/tests/unit/sync/syncPeerBroadcast.test.ts`

- [ ] **Step 1: 在 `SyncEngine` 暴露 `triggerSync()`**

```typescript
// In SyncEngine
public triggerSync(): void {
  if (!this.session.isReady()) return;
  this.localManifestSent = false;
  this.remoteManifestReceived = false;
  this.pendingRequests.clear();
  this.pendingAcks.clear();
  this.completed = false;
  this.sendManifest();
}
```

- [ ] **Step 2: 在 `SyncPeerManager` 添加 `broadcastLocalChange()`**

```typescript
broadcastLocalChange(): void {
  for (const peer of this.getPeers()) {
    if (peer.state === 'idle' || peer.state === 'syncing') {
      peer.state = 'syncing';
      peer.engine.triggerSync();
    }
  }
  this.emit('stateChanged', this.getState());
}
```

- [ ] **Step 3: 编写广播测试**

创建两个 mock peer，调用 `broadcastLocalChange()`，验证两个 engine 的 `triggerSync` 都被调用。

- [ ] **Step 4: 运行测试**

```bash
cd /workspace/taskflow/desktop
npx vitest run src/tests/unit/sync/syncPeerBroadcast.test.ts
```
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/main/services/sync/syncPeerManager.ts desktop/src/main/services/sync/syncEngine.ts desktop/src/tests/unit/sync/syncPeerBroadcast.test.ts
git commit -m "feat(sync): broadcast local changes to all online peers"
```

---

## Task 3: 连接调度器 `SyncScheduler`

**Files:**
- Create: `desktop/src/main/services/sync/syncScheduler.ts`
- Create: `desktop/src/tests/unit/sync/syncScheduler.test.ts`

- [ ] **Step 1: 创建 `SyncScheduler`**

```typescript
import { EventEmitter } from 'events';
import { SyncPeerManager } from './syncPeerManager';

export interface SyncSchedulerOptions {
  peerManager: SyncPeerManager;
  intervalMs?: number;
}

export class SyncScheduler extends EventEmitter {
  private peerManager: SyncPeerManager;
  private intervalMs: number;
  private timer: NodeJS.Timeout | null = null;

  constructor(opts: SyncSchedulerOptions) {
    super();
    this.peerManager = opts.peerManager;
    this.intervalMs = opts.intervalMs ?? 30_000;
  }

  start(): void {
    if (this.timer) return;
    this.timer = setInterval(() => this.tick(), this.intervalMs);
  }

  stop(): void {
    if (this.timer) {
      clearInterval(this.timer);
      this.timer = null;
    }
  }

  tick(): void {
    this.peerManager.broadcastLocalChange();
  }

  triggerNow(): void {
    this.tick();
  }
}
```

- [ ] **Step 2: 编写调度器测试**

验证 start/stop/tick 调用 peerManager.broadcastLocalChange。

- [ ] **Step 3: 运行测试**

```bash
cd /workspace/taskflow/desktop
npx vitest run src/tests/unit/sync/syncScheduler.test.ts
```
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/main/services/sync/syncScheduler.ts desktop/src/tests/unit/sync/syncScheduler.test.ts
git commit -m "feat(sync): add SyncScheduler for periodic multi-peer sync"
```

---

## Task 4: 版本向量检测并发写入

**Files:**
- Modify: `desktop/src/main/services/sync/conflictResolver.ts`
- Modify: `desktop/src/main/services/sync/syncEngine.ts`
- Modify: `desktop/src/main/services/sync/syncStorage.ts`
- Create: `desktop/src/tests/unit/sync/conflictResolverVersionVector.test.ts`

- [ ] **Step 1: 在 `syncStorage.ts` 扩展 sync_records 表**

新增 `device_version` 列：

```sql
ALTER TABLE sync_records ADD COLUMN device_version TEXT DEFAULT '{}';
```

在迁移脚本中新增该列。

- [ ] **Step 2: 扩展 `resolveConflict` 支持 version vector**

```typescript
export interface VersionVector {
  [deviceId: string]: number;
}

export interface ConflictRecord {
  id: string;
  updatedAt: number;
  version: number;
  deviceVersion?: VersionVector;
}

export type ConflictDecision = 'local' | 'remote' | 'concurrent';

export function resolveConflict(
  local: ConflictRecord,
  remote: ConflictRecord
): ConflictDecision {
  if (local.updatedAt > remote.updatedAt) return 'local';
  if (local.updatedAt < remote.updatedAt) return 'remote';

  const localVv = local.deviceVersion ?? {};
  const remoteVv = remote.deviceVersion ?? {};

  const localDominates = Object.entries(localVv).every(
    ([deviceId, version]) => (remoteVv[deviceId] ?? 0) <= version
  );
  const remoteDominates = Object.entries(remoteVv).every(
    ([deviceId, version]) => (localVv[deviceId] ?? 0) <= version
  );

  if (localDominates && !remoteDominates) return 'local';
  if (remoteDominates && !localDominates) return 'remote';
  return 'concurrent';
}
```

- [ ] **Step 3: 在 `SyncEngine.handleBatch` 中处理 `concurrent` 决策**

当决策为 `concurrent` 时，仍按 last-write-wins 应用 remote，但 emit `concurrentWrite` 事件，携带 record id 与两个 version vector。

- [ ] **Step 4: 编写版本向量测试**

验证 dominance 判断与并发检测。

- [ ] **Step 5: 运行测试**

```bash
cd /workspace/taskflow/desktop
npx vitest run src/tests/unit/sync/conflictResolverVersionVector.test.ts
```
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/main/services/sync/conflictResolver.ts desktop/src/main/services/sync/syncEngine.ts desktop/src/main/services/sync/syncStorage.ts desktop/src/tests/unit/sync/conflictResolverVersionVector.test.ts
git commit -m "feat(sync): add version vector concurrent-write detection"
```

---

## Task 5: UI 展示多设备同步状态

**Files:**
- Modify: `desktop/src/renderer/store/syncStore.ts`
- Modify: `desktop/src/renderer/components/sync/DeviceList.tsx`
- Modify: `desktop/src/main/ipc/syncChannels.ts`
- Modify: `desktop/src/shared/constants.ts`
- Modify: `desktop/src/shared/types.ts`
- Modify: `desktop/src/preload/index.ts`

- [ ] **Step 1: 在 `shared/types.ts` 添加 `PeerState` 类型**

```typescript
export type PeerState = 'connecting' | 'handshaking' | 'syncing' | 'idle' | 'error' | 'closed';

export interface SyncPeerInfo {
  deviceId: string;
  state: PeerState;
  lastSyncAt: number | null;
  error: string | null;
}
```

- [ ] **Step 2: 在 `constants.ts` 添加状态广播通道**

```typescript
export const SYNC_CHANNELS = {
  // ... existing channels
  ON_PEER_STATE_CHANGED: 'sync:onPeerStateChanged',
} as const;
```

- [ ] **Step 3: 在 `syncChannels.ts` 注册并广播 peer 状态**

`SyncPeerManager` 的 `stateChanged` 事件触发 `webContents.send(SYNC_CHANNELS.ON_PEER_STATE_CHANGED, peers)`。

- [ ] **Step 4: 更新 `preload/index.ts` 暴露 `onPeerStateChanged`**

- [ ] **Step 5: 更新 `syncStore.ts` 与 `DeviceList.tsx` 展示状态**

每台设备旁显示：在线/同步中/错误、上次同步时间、错误信息。

- [ ] **Step 6: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/shared/types.ts desktop/src/shared/constants.ts desktop/src/main/ipc/syncChannels.ts desktop/src/preload/index.ts desktop/src/renderer/store/syncStore.ts desktop/src/renderer/components/sync/DeviceList.tsx
git commit -m "feat(sync): display per-device sync state in settings UI"
```

---

## Task 6: 集成测试——三台设备并发写入

**Files:**
- Create: `desktop/src/tests/integration/sync/multiPeerSync.integration.test.ts`

- [ ] **Step 1: 创建集成测试**

1. 启动真实 relay。
2. 创建三个内存数据库实例 A、B、C，两两配对。
3. A 写入任务，广播给 B 和 C。
4. 验证 B 和 C 都收到记录。
5. B 和 C 同时写入同一任务的不同版本，触发并发检测。

- [ ] **Step 2: 运行测试**

```bash
cd /workspace/taskflow/desktop
npx vitest run src/tests/integration/sync/multiPeerSync.integration.test.ts
```
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
cd /workspace/taskflow
git add desktop/src/tests/integration/sync/multiPeerSync.integration.test.ts
git commit -m "test(sync): add three-device concurrent sync integration test"
```

---

## Task 7: 全量验证

- [ ] **Step 1: 桌面端全量检查**

```bash
cd /workspace/taskflow/desktop
npm run typecheck
npm run lint
npm run test
npm run build
```
Expected: 全部通过。

- [ ] **Step 2: relay 全量检查**

```bash
cd /workspace/taskflow/relay
npm run typecheck
npm run test
npm run build
```
Expected: 全部通过。

- [ ] **Step 3: 后端全量检查**

```bash
cd /workspace/taskflow/backend
PYTHONPATH=. pytest -q
```
Expected: PASS。

---

## Spec Coverage Check

| 设计需求 | 覆盖任务 |
|---|---|
| 同时维护多个同步会话 | Task 1 |
| 本地变更广播给所有在线设备 | Task 2 |
| 定时/触发式同步调度 | Task 3 |
| 并发写入检测 | Task 4 |
| UI 展示每台设备状态 | Task 5 |
| 多设备集成验证 | Task 6 |

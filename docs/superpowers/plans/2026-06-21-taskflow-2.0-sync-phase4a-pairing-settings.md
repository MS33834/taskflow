# TaskFlow 2.0 Sync Phase 4a: 配对与设置集成实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` or dispatch a fresh `general_purpose_task` subagent per Task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在桌面端实现同步设置页、新设备配对码流程，并通过 relay 完成 SMK 安全传输。

**Architecture:** 在主进程新增 `relayClient` 与 `pairingService`，通过新增 IPC 通道暴露给渲染层；渲染层使用 `syncStore` 管理状态，并在设置页展示同步开关、中继地址、设备列表与配对弹窗。配对成功后，双方把对方公钥写入 `sync_devices`。

**Tech Stack:** Electron、TypeScript、React、Zustand、WebSocket、`ws`、`better-sqlite3-multiple-ciphers`、vitest。

---

## 前置检查

- [ ] **Step 1: 确认基础环境健康**

Run:
```bash
cd /workspace/github-taskflow/desktop
npm run typecheck
npm run lint
npm run test
```
Expected: 全部通过（当前 17 files / 111 tests PASS）。

---

## Task 1: 新增 `SMK_TRANSFER` 消息类型

**Files:**
- Modify: `desktop/src/main/services/sync/syncMessages.ts`
- Modify: `desktop/src/main/services/sync/syncSession.ts`
- Test: `desktop/src/tests/unit/sync/syncMessages.test.ts`

- [ ] **Step 1: 在 `syncMessages.ts` 添加 `SMK_TRANSFER` 类型与守卫**

在 `SyncMessage` 联合类型中追加：

```typescript
| { type: 'SMK_TRANSFER'; encryptedSmk: string }
```

并添加守卫函数：

```typescript
export function isSmkTransferMessage(msg: unknown): msg is { type: 'SMK_TRANSFER'; encryptedSmk: string } {
  return isRecord(msg) && msg.type === 'SMK_TRANSFER' && typeof msg.encryptedSmk === 'string';
}
```

在 `deserializeMessage` 的分支中处理 `SMK_TRANSFER`。

- [ ] **Step 2: 运行 `syncMessages` 测试**

Run:
```bash
cd /workspace/github-taskflow/desktop
npx vitest run src/tests/unit/sync/syncMessages.test.ts
```
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/main/services/sync/syncMessages.ts
git commit -m "feat(sync): add SMK_TRANSFER message type"
```

---

## Task 2: 实现 relay HTTP 客户端 `relayClient.ts`

**Files:**
- Create: `desktop/src/main/services/sync/relayClient.ts`
- Create: `desktop/src/tests/unit/sync/relayClient.test.ts`

- [ ] **Step 1: 创建 `relayClient.ts`**

```typescript
import { sign } from 'crypto';
import { generateDeviceIdentity, type DeviceIdentity } from './syncIdentity';
import { buildAuthMessage } from '../../../../../relay/src/auth';

export interface RelayRegisterResult {
  deviceId: string;
  token: string;
  wsUrl: string;
}

export interface RelayPairingCodeResult {
  code: string;
  expiresAt: number;
}

export class RelayClient {
  private baseUrl: string;

  constructor(baseUrl: string) {
    this.baseUrl = baseUrl.replace(/\/$/, '');
  }

  async registerDevice(identity: DeviceIdentity): Promise<RelayRegisterResult> {
    const timestamp = Math.floor(Date.now() / 1000);
    const message = buildAuthMessage(identity.deviceId, timestamp, 'register');
    const signature = sign(null, message, identity.privateKeyPem).toString('base64');
    const res = await fetch(`${this.baseUrl}/register-device`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        deviceId: identity.deviceId,
        publicKey: identity.publicKeyPem,
        timestamp,
        signature,
      }),
    });
    if (!res.ok) {
      throw new Error(`register-device failed: ${res.status} ${await res.text()}`);
    }
    return res.json() as Promise<RelayRegisterResult>;
  }

  async createPairingCode(identity: DeviceIdentity, token: string): Promise<RelayPairingCodeResult> {
    const timestamp = Math.floor(Date.now() / 1000);
    const message = buildAuthMessage(identity.deviceId, timestamp, 'pairing-code');
    const signature = sign(null, message, identity.privateKeyPem).toString('base64');
    const res = await fetch(`${this.baseUrl}/pairing-codes`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({ timestamp, signature }),
    });
    if (!res.ok) {
      throw new Error(`pairing-codes failed: ${res.status} ${await res.text()}`);
    }
    return res.json() as Promise<RelayPairingCodeResult>;
  }

  async claimPairingCode(
    identity: DeviceIdentity,
    code: string
  ): Promise<RelayRegisterResult> {
    const timestamp = Math.floor(Date.now() / 1000);
    const message = buildAuthMessage(identity.deviceId, timestamp, 'claim-pairing-code');
    const signature = sign(null, message, identity.privateKeyPem).toString('base64');
    const res = await fetch(`${this.baseUrl}/claim-pairing-code`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        code,
        deviceId: identity.deviceId,
        publicKey: identity.publicKeyPem,
        timestamp,
        signature,
      }),
    });
    if (!res.ok) {
      throw new Error(`claim-pairing-code failed: ${res.status} ${await res.text()}`);
    }
    return res.json() as Promise<RelayRegisterResult>;
  }
}
```

- [ ] **Step 2: 创建 `relayClient.test.ts`**

使用 `vi.fn()` mock `fetch`，验证请求体字段与 Authorization header。

- [ ] **Step 3: 运行测试**

Run:
```bash
cd /workspace/github-taskflow/desktop
npx vitest run src/tests/unit/sync/relayClient.test.ts
```
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/main/services/sync/relayClient.ts desktop/src/tests/unit/sync/relayClient.test.ts
git commit -m "feat(sync): add relay HTTP client with signed requests"
```

---

## Task 3: 实现 `pairingService.ts`

**Files:**
- Create: `desktop/src/main/services/sync/pairingService.ts`
- Create: `desktop/src/tests/unit/sync/pairingService.test.ts`

- [ ] **Step 1: 实现旧设备侧生成配对码**

```typescript
import { RelayClient } from './relayClient';
import { generateDeviceIdentity, type DeviceIdentity } from './syncIdentity';
import { loadSyncMasterKey, saveSyncMasterKey } from './syncCrypto';
import { registerSyncDevice } from './syncStorage';

export interface PairingCodeResult {
  code: string;
  expiresAt: number;
}

export async function generatePairingCode(
  relayUrl: string,
  identity: DeviceIdentity,
  tokenStorage: { get: () => string | undefined; set: (value: string) => void }
): Promise<PairingCodeResult> {
  const client = new RelayClient(relayUrl);
  let token = tokenStorage.get();
  if (!token) {
    const result = await client.registerDevice(identity);
    token = result.token;
    tokenStorage.set(token);
  }
  return client.createPairingCode(identity, token);
}
```

- [ ] **Step 2: 实现新设备侧认领配对码并接收 SMK**

使用 `RelayTransport` + `SyncSession` 完成握手，并在 `ready` 后发送/接收 `SMK_TRANSFER`。

旧设备侧 `respondToPairing`：启动 responder `RelayTransport`，在 `SyncSession` `ready` 后发送 SMK。

新设备侧 `claimPairingCodeAndPair`：claim code 后作为 initiator 连接，收到 `SMK_TRANSFER` 后保存 SMK 并注册旧设备。

- [ ] **Step 3: 创建配对服务单元测试**

Mock `RelayClient`、`SyncSession`、`RelayTransport`，验证调用顺序与 SMK 保存。

- [ ] **Step 4: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/main/services/sync/pairingService.ts desktop/src/tests/unit/sync/pairingService.test.ts
git commit -m "feat(sync): add pairing service for host and joiner flows"
```

---

## Task 4: 新增同步 IPC 通道

**Files:**
- Create: `desktop/src/main/ipc/syncChannels.ts`
- Modify: `desktop/src/preload/index.ts`
- Modify: `desktop/src/shared/constants.ts`
- Modify: `desktop/src/main/index.ts`
- Modify: `desktop/src/shared/types.ts`

- [ ] **Step 1: 在 `constants.ts` 定义通道名**

```typescript
export const SYNC_CHANNELS = {
  GET_STATE: 'sync:getState',
  SET_ENABLED: 'sync:setEnabled',
  SET_RELAY_URL: 'sync:setRelayUrl',
  GENERATE_PAIRING_CODE: 'sync:generatePairingCode',
  CLAIM_PAIRING_CODE: 'sync:claimPairingCode',
  REMOVE_DEVICE: 'sync:removeDevice',
  ON_STATE_CHANGED: 'sync:onStateChanged',
} as const;
```

- [ ] **Step 2: 创建 `syncChannels.ts` 注册 IPC 处理器**

```typescript
import { ipcMain } from 'electron';
import { SYNC_CHANNELS } from '../../shared/constants';

export function registerSyncIpc() {
  ipcMain.handle(SYNC_CHANNELS.GET_STATE, async () => {
    // 读取本地同步设置与设备列表
  });
  ipcMain.handle(SYNC_CHANNELS.SET_ENABLED, async (_event, enabled: boolean) => {
    // 启用/禁用同步
  });
  ipcMain.handle(SYNC_CHANNELS.SET_RELAY_URL, async (_event, url: string) => {
    // 验证并保存中继地址
  });
  ipcMain.handle(SYNC_CHANNELS.GENERATE_PAIRING_CODE, async () => {
    // 调用 pairingService.generatePairingCode
  });
  ipcMain.handle(SYNC_CHANNELS.CLAIM_PAIRING_CODE, async (_event, code: string) => {
    // 调用 pairingService.claimPairingCodeAndPair
  });
  ipcMain.handle(SYNC_CHANNELS.REMOVE_DEVICE, async (_event, deviceId: string) => {
    // 移除设备
  });
}
```

- [ ] **Step 3: 在 `preload/index.ts` 暴露 API**

```typescript
sync: {
  getState: () => ipcRenderer.invoke(SYNC_CHANNELS.GET_STATE),
  setEnabled: (enabled: boolean) => ipcRenderer.invoke(SYNC_CHANNELS.SET_ENABLED, enabled),
  setRelayUrl: (url: string) => ipcRenderer.invoke(SYNC_CHANNELS.SET_RELAY_URL, url),
  generatePairingCode: () => ipcRenderer.invoke(SYNC_CHANNELS.GENERATE_PAIRING_CODE),
  claimPairingCode: (code: string) => ipcRenderer.invoke(SYNC_CHANNELS.CLAIM_PAIRING_CODE, code),
  removeDevice: (deviceId: string) => ipcRenderer.invoke(SYNC_CHANNELS.REMOVE_DEVICE, deviceId),
  onStateChanged: (callback: (state: SyncState) => void) => {
    const listener = (_event: IpcRendererEvent, state: SyncState) => callback(state);
    ipcRenderer.on(SYNC_CHANNELS.ON_STATE_CHANGED, listener);
    return () => ipcRenderer.off(SYNC_CHANNELS.ON_STATE_CHANGED, listener);
  },
}
```

- [ ] **Step 4: 更新类型与入口注册**

在 `shared/types.ts` 添加 `SyncState` 类型；在 `main/index.ts` 调用 `registerSyncIpc()`。

- [ ] **Step 5: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/main/ipc/syncChannels.ts desktop/src/preload/index.ts desktop/src/shared/constants.ts desktop/src/shared/types.ts desktop/src/main/index.ts
git commit -m "feat(sync): add sync IPC channels and preload API"
```

---

## Task 5: 实现 `syncStore.ts` 与设置页同步 UI

**Files:**
- Create: `desktop/src/renderer/store/syncStore.ts`
- Create: `desktop/src/renderer/components/sync/SyncSettingsPanel.tsx`
- Create: `desktop/src/renderer/components/sync/PairingDialog.tsx`
- Create: `desktop/src/renderer/components/sync/DeviceList.tsx`
- Modify: `desktop/src/renderer/pages/SettingsPage.tsx`

- [ ] **Step 1: 创建 `syncStore.ts`**

使用 Zustand，管理 `enabled`、`relayUrl`、`devices`、`pairingCode`、`error`。

- [ ] **Step 2: 创建 `SyncSettingsPanel.tsx`**

包含：同步开关、中继地址输入、设备列表、添加设备按钮。

- [ ] **Step 3: 创建 `PairingDialog.tsx`**

两种模式：
- Host 模式：展示生成的 8 位配对码与倒计时。
- Join 模式：输入配对码，点击加入。

- [ ] **Step 4: 创建 `DeviceList.tsx`**

展示已配对设备 ID、名称、上次在线时间、移除按钮。

- [ ] **Step 5: 在 `SettingsPage.tsx` 引入 `SyncSettingsPanel`**

在安全/外观/数据之后新增「同步」折叠面板。

- [ ] **Step 6: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/renderer/store/syncStore.ts desktop/src/renderer/components/sync/
desktop/src/renderer/pages/SettingsPage.tsx
git commit -m "feat(sync): add sync settings UI and store"
```

---

## Task 6: 实现 token 与同步设置的持久化

**Files:**
- Create: `desktop/src/main/services/sync/syncSettingsState.ts`
- Modify: `desktop/src/main/services/sync/syncStorage.ts`
- Modify: `desktop/src/main/ipc/syncChannels.ts`

- [ ] **Step 1: 创建 `syncSettingsState.ts`**

使用 `safeStorage` 保存 relay URL、token、enabled 标志；提供 `getSyncSettings()` / `setSyncSettings()`。

- [ ] **Step 2: 在 `syncStorage.ts` 添加设备列表读取辅助函数**

如需要，新增 `listSyncDevices()`（如已存在则直接使用）。

- [ ] **Step 3: 在 `syncChannels.ts` 中接入持久化**

`setEnabled`、`setRelayUrl` 写入 `syncSettingsState`；`getState` 从持久化读取。

- [ ] **Step 4: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/main/services/sync/syncSettingsState.ts desktop/src/main/services/sync/syncStorage.ts desktop/src/main/ipc/syncChannels.ts
git commit -m "feat(sync): persist sync settings and relay token"
```

---

## Task 7: 集成测试——两台设备通过 relay 配对

**Files:**
- Create: `desktop/src/tests/integration/sync/pairing.integration.test.ts`

- [ ] **Step 1: 创建集成测试**

启动真实 `createRelayServer`，创建两个内存数据库实例：
1. Host 注册并生成配对码。
2. Joiner claim 配对码并连接。
3. 双方完成 SyncSession 握手。
4. Joiner 收到 SMK 并保存。
5. 双方 `sync_devices` 包含对方。

- [ ] **Step 2: 运行集成测试**

Run:
```bash
cd /workspace/github-taskflow/desktop
npx vitest run src/tests/integration/sync/pairing.integration.test.ts
```
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
cd /workspace/github-taskflow
git add desktop/src/tests/integration/sync/pairing.integration.test.ts
git commit -m "test(sync): add pairing integration test via relay"
```

---

## Task 8: 全量验证

- [ ] **Step 1: 桌面端全量检查**

Run:
```bash
cd /workspace/github-taskflow/desktop
npm run typecheck
npm run lint
npm run test
npm run build
```
Expected: 全部通过。

- [ ] **Step 2: relay 全量检查**

Run:
```bash
cd /workspace/github-taskflow/relay
npm run typecheck
npm run test
npm run build
```
Expected: 全部通过。

- [ ] **Step 3: 后端全量检查**

Run:
```bash
cd /workspace/github-taskflow/backend
PYTHONPATH=. pytest -q
```
Expected: PASS。

---

## Spec Coverage Check

| 设计需求 | 覆盖任务 |
|---|---|
| 设置页同步开关/中继地址 | Task 5 |
| 旧设备生成配对码 | Task 2 + Task 3 |
| 新设备输入配对码并接收 SMK | Task 1 + Task 3 |
| 设备列表/移除设备 | Task 5 + Task 6 |
| Token 与设置持久化 | Task 6 |
| 配对成功后写入 sync_devices | Task 3 |
| 测试覆盖 | Task 2/3/4/7 |

